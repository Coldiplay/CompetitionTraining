<Window x:Class="CompetitionTraining2.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CompetitionTraining2"
        xmlns:oxy="http://oxyplot.org/wpf"
        mc:Ignorable="d"
        Title="MainWindow" Height="550" Width="1000">

    <Window.Resources>
        <SolidColorBrush x:Key="backBrush" Color="#FF0F2233"/>

        <Style TargetType="Button" x:Key="ButtonWOBackgroundChange">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" 
                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                              />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True"/>
            </Style.Triggers>
        </Style>
        <!--<Style >
            <Setter Property="Background" Value="#FF0F2233"/>
        </Style>-->
    </Window.Resources>

    <Grid>
       
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="0.3*"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Grid.ColumnSpan="2">
            <Image/>
            <Label FontSize="14" Content="Лого"/>
            <Separator/>
            <StackPanel Orientation="Horizontal">
                <Image Source="/Assets/Icons/Font-Awesome-SVG-PNG/black/png/128/eur.png" /> <!--флаг-->
                <StackPanel>
                    <Label Content="{Binding UserName}"/>
                    <Label Content="{Binding UserRole}"/>
                    
                </StackPanel>
                
            </StackPanel>
        </StackPanel>


        <StackPanel Grid.Row="1" >
            <StackPanel.Resources>
                <Style TargetType="Button" x:Key="DefaultButton" BasedOn="{StaticResource ButtonWOBackgroundChange}">
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="BorderThickness" Value="0"/>
                    <Setter Property="Margin" Value="2"/>
                    <Setter Property="Foreground" Value="White"/>
                </Style>

                <Style TargetType="Button" BasedOn="{StaticResource DefaultButton}">
                    <Setter Property="HorizontalContentAlignment" Value="Left"/>
                </Style>
                
                <Style TargetType="StackPanel">
                    <Setter Property="Background" Value="{StaticResource backBrush}"/>
                    <Style.Resources>
                        <Style TargetType="Button" BasedOn="{StaticResource DefaultButton}">
                            <Setter Property="HorizontalContentAlignment" Value="Left"/>
                            <Setter Property="Margin" Value="50 2 2 2"/>
                        </Style>
                    </Style.Resources>
                </Style>

                <Style TargetType="Image">
                    <Setter Property="Height" Value="16"/>
                </Style>
                
                <Style TargetType="Label">
                    <Setter Property="Foreground" Value="White"/>
                    <Setter Property="Background" Value="Transparent"/>
                </Style>
            </StackPanel.Resources>


            <Label Content="Навигация"/>

            <Button Click="MainPageShow">
                <Button.Content>
                    <StackPanel Orientation="Horizontal">
                        <Image Source="/Assets/Icons/Font-Awesome-SVG-PNG/black/png/128/search.png" Height="16"/>
                        <Label Content="Главная"/>
                    </StackPanel>
                </Button.Content>
            </Button>

            <Button Click="MonitorVendMachinesShow">
                <Button.Content>
                    <StackPanel Orientation="Horizontal">
                        <Image Source="/Assets/Icons/Font-Awesome-SVG-PNG/black/png/128/television.png"/>
                        <Label Content="Монитор ТА"/>
                    </StackPanel>
                </Button.Content>
            </Button>

            <Button Click="ReportsVisChange">
                <Button.Content>
                    <StackPanel Orientation="Horizontal">
                        <Image Source="/Assets/Icons/Font-Awesome-SVG-PNG/black/png/128/file-text.png" />
                        <Label Content="Детальные отчёты"/>
                    </StackPanel>
                </Button.Content>
            </Button>
            <StackPanel Visibility="{Binding ReportsVis}">
                <Button Content="Отчёты 1" />
                <Button Content="Отчёты 2"/>
            </StackPanel>


            <Button Click="AccountingVisChange">
                <Button.Content>
                    <StackPanel Orientation="Horizontal">
                        <Image Source="/Assets/Icons/Font-Awesome-SVG-PNG/black/png/128/shopping-cart.png"/>
                        <Label Content="Учёт ТМЦ"/>
                    </StackPanel>
                </Button.Content>
            </Button>
            <StackPanel Visibility="{Binding AccountingVis}">
                <Button Content="Учёт 1" />
                <Button Content="Учёт 2" />
            </StackPanel>


            <Button Click="AdministrationVisChange">
                <Button.Content>
                    <StackPanel Orientation="Horizontal">
                        <Image Source="/Assets/Icons/Font-Awesome-SVG-PNG/black/png/16/cogs.png"/>
                        <Label Content="Администрирование"/>
                    </StackPanel>
                </Button.Content>
            </Button>
            <StackPanel Visibility="{Binding AdministrationVis}">
                <Button Content="Торговые автоматы" />
                <Button Content="Компании" />
                <Button Content="Пользователи" />
                <Button Content="Модемы" />
                <Button Content="Дополнительные" />
            </StackPanel>
            

        </StackPanel>

        <Grid Grid.Row="1" Grid.Column="1" Background="#FFC1C1C1">

            <Grid.RowDefinitions>
                <RowDefinition Height="30"/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>

            <Grid Grid.ColumnSpan="2" Background="{StaticResource backBrush}">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>
                <Label Foreground="White" Content="{Binding CompanyName}" HorizontalAlignment="Left" VerticalAlignment="Center"/>
                <Label Foreground="Gray" Grid.Column="1" HorizontalAlignment="Right" VerticalAlignment="Center"/>
            </Grid>
            

            <ContentControl Content="{Binding Test}"/>


            <Grid Grid.ColumnSpan="2" Grid.Row="1">
                <Grid.Resources>
                    <Style TargetType="Grid">
                        <Setter Property="Margin" Value="10"/>
                    </Style>
                </Grid.Resources>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="40"/>
                    <RowDefinition />
                    <RowDefinition />
                </Grid.RowDefinitions>


                <Label Margin="10 0 0 0" VerticalAlignment="Center" FontSize="20" Content="Личный кабинет. Главная" Grid.ColumnSpan="3"/>

                <Grid Grid.Row="1">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="0.2*"/>
                        <RowDefinition/>
                    </Grid.RowDefinitions>

                    <Label Content="Эффективность сети" FontWeight="SemiBold" Background="LightGray"/>

                    <Grid Grid.Row="1" Margin="0" Background="White">
                        
                    </Grid>
                </Grid>


                <Grid Grid.Row="1" Grid.Column="1">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="0.2*"/>
                        <RowDefinition/>
                    </Grid.RowDefinitions>

                    <Label FontWeight="SemiBold" Content="Состояние сети" Background="LightGray"/>

                    <Grid Grid.Row="1" Margin="0" Background="White">

                    </Grid>

                </Grid>


                <Grid Grid.Row="1" Grid.Column="2">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="0.2*" />
                        <RowDefinition />
                    </Grid.RowDefinitions>


                    <Label Content="Сводка" FontWeight="SemiBold" Background="LightGray"/>

                    <Grid Grid.Row="1" Margin="0" Background="White">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition/>
                            <ColumnDefinition/>
                        </Grid.ColumnDefinitions>

                        <StackPanel>
                            <StackPanel.Resources>
                                <Style TargetType="Label">
                                    <Setter Property="FontSize" Value="8"/>
                                    <Setter Property="HorizontalAlignment" Value="Left"/>
                                    <Setter Property="Padding" Value="4"/>
                                </Style>
                            </StackPanel.Resources>
                            
                            <Label>Денег в ТА</Label>
                            <Label>Сдача в ТА</Label>
                            <Label>Выручка, сегодня</Label>
                            <Label>Выручка, вчера</Label>
                            <Label>Инкасированно, сегодня</Label>
                            <Label>Инкасированно, вчера</Label>
                            <Label>Обслужено ТА, сег./вчера</Label>
                        </StackPanel>

                        <StackPanel Grid.Column="1">
                            <StackPanel.Resources>
                                <Style TargetType="Label">
                                    <Setter Property="FontSize" Value="8"/>
                                    <Setter Property="HorizontalAlignment" Value="Right"/>
                                    <Setter Property="Padding" Value="4"/>
                                </Style>
                            </StackPanel.Resources>
                            
                            <Label>46546 р.</Label>
                            <Label>???465 р.</Label>
                            <Label Content="{Binding RevenueToday}"/>
                            <Label Content="{Binding RevenueYesterday}"/>
                            <Label>???46546 р.</Label>
                            <Label>???465 р.</Label>
                            
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                <Label Content="{Binding MaintenancesToday}"/>
                                <Label>/</Label>
                                <Label Content="{Binding MaintenancesYesterday}"/>
                            </StackPanel>
                        </StackPanel>
                    </Grid>
                </Grid>
                
                
                

                <Grid Grid.Row="2" Grid.ColumnSpan="2">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="0.7*"/>
                        <RowDefinition/>
                    </Grid.RowDefinitions>


                    <StackPanel Background="LightGray">
                        <Label Content="Динамика продаж за последние 10 дней" FontWeight="SemiBold" />
                        <TextBlock Text="{Binding SalesText}"/>

                        <StackPanel Orientation="Horizontal" >
                            <Button Content="По сумме" Margin="10 10 3 10"/>
                            <Button Content="По количеству" Margin="3 10 10 10"/>
                        </StackPanel>

                    </StackPanel>
                    <oxy:PlotView Model="{Binding Sales}" Grid.Row="1" DefaultTrackerTemplate="{x:Null}"/>

                </Grid>

                <Grid Grid.Row="2" Grid.Column="2">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="0.2*"/>
                        <RowDefinition/>
                    </Grid.RowDefinitions>

                    <Label Content="Новости" Background="LightGray" FontWeight="SemiBold"/>

                    <ListView Grid.Row="1">
                        
                    </ListView>
                    

                </Grid>
                
            </Grid>


        </Grid>

        
        
        
    </Grid>
</Window>
